<div class="flex justify-between gap-2 mt-5 mb-1">
  <div class="shrink">
    <input
      type="text"
      class="search w-full bg-gray-700 text-gray-300 placeholder-gray-400 border border-gray-500 rounded-md"
      placeholder="Поиск..."
    />
  </div>
  <div class="flex gap-2 shrink-0">
    <button
      type="button"
      class="sort asc p-3 rounded-md text-sm font-semibold transition-all bg-blue-800 hover:bg-blue-700 text-gray-200"
      data-sort="name">Имя</button
    >
    <!-- <button
      type="button"
      class="sort p-3 rounded-md text-sm font-semibold transition-all bg-blue-800 hover:bg-blue-700 text-gray-200"
      data-sort="iso">Дата</button
    > -->
  </div>
</div>

<style>
  .sort.asc::after {
    content: "▲";
  }
  .sort.desc::after {
    content: "▼";
  }
</style>